<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/fragmentFeed">

    <fragment
        android:id="@+id/fragmentFeed"
        android:name="ru.netology.ynew.activity.activity.FragmentFeed"
        android:label="fragment_feed"
        tools:layout="@layout/fragment_feed" >
        <action
            android:id="@+id/action_fragmentFeed_to_fragmentFavorite"
            app:destination="@id/fragmentFavorite" />
    </fragment>
    <fragment
        android:id="@+id/fragmentFavorite"
        android:name="ru.netology.ynew.activity.activity.FragmentFavorite"
        android:label="FragmentFavorite" />
</navigation>